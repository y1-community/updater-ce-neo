/* Fallback / supplement — the primary stylesheet is generated by dark.py.
   This file is loaded by MainWindow._apply_style() only if the dynamically
   generated QSS is absent.  Keep rules minimal. */
